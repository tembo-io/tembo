CREATE TABLE extensions (
    id INT4,
    name VARCHAR,
    updated_at TIMESTAMP,
    created_at TIMESTAMP,
    downloads INT4,
    description VARCHAR,
    homepage VARCHAR,
    documentation VARCHAR,
    repository VARCHAR
);
