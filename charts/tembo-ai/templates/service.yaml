apiVersion: v1
kind: Service
metadata:
  name: {{ include "tembo-ai.fullname" . }}
  labels:
    {{- include "tembo-ai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tembo-ai.selectorLabels" . | nindent 4 }}
