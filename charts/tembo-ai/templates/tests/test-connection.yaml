apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tembo-ai.fullname" . }}-test-connection"
  labels:
    {{- include "tembo-ai.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tembo-ai.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
